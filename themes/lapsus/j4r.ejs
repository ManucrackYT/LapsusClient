<!DOCTYPE html>
<html lang="en">
<head>
    <title><%= settings.name %></title>
    <link rel="icon" type="image/png" href="<%= settings.logo.url %>">
    <link rel="stylesheet" href="../assets/tailwind.css">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <script src="https://unpkg.com/@popperjs/core@2.10.1/dist/umd/popper.min.js"></script>
    
    <!-- Font Awesome Import -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    
    <% if (settings.music == true) { %>
    <!-- Music Player -->
    <script src="https://static.elfsight.com/platform/platform.js" data-use-service-core defer></script>
    <div class="elfsight-app-9c8f4667-f5ce-46b9-99e2-f32095e11518" data-elfsight-app-lazy></div>
    <% } %>
  
    <!-- Inter font -->
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        body {
            font-family: Arial, Helvetica, sans-serif;
        }
  
        /* The Modal (background) */
        .modal {
            position: fixed;
            /* Stay in place */
            z-index: 1;
            /* Sit on top */
            padding-top: 100px;
            /* Location of the box */
            left: 0;
            top: 0;
            width: 100%;
            /* Full width */
            height: 100%;
            /* Full height */
            overflow: auto;
            /* Enable scroll if needed */
            background-color: rgb(0, 0, 0);
            /* Fallback color */
            background-color: rgba(0, 0, 0, 0.4);
            /* Black w/ opacity */
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            transition: 0.5s;
        }
  
        /* Modal Content */
        .modal-content {
            background-color: #fefefe;
            margin: auto;
            padding: 40px;
            width: 30%;
            border-radius: 16px;
            margin-top: 12%;
            transition: 0.5s
        }
  
        .close:hover,
        .close:focus {
            color: #000;
            text-decoration: none;
            cursor: pointer;
            transition: 0.5s
        }
        
        .server-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease;
            height: 100%; /* Ensure all cards have equal height */
        }
        
        .server-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
        }
        
        .coin-badge {
            display: inline-flex;
            align-items: center;
            background: linear-gradient(to right, #FFD700, #FFC400);
            color: #333;
            font-weight: 600;
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 0.75rem; /* Smaller font for badge */
        }
        
        /* Compact card styling */
        .compact-card {
            padding: 1.25rem; /* Slightly increased padding for better spacing */
        }
        
        .compact-title {
            font-size: 1rem; /* Smaller title */
            margin-bottom: 0.75rem; /* Increased spacing */
        }
        
        .compact-text {
            font-size: 0.875rem; /* Smaller text */
            margin-bottom: 1.25rem; /* Increased spacing */
            line-height: 1.5; /* Better line height for readability */
        }
        
        .compact-button {
            padding: 0.5rem 1.25rem; /* Slightly larger button */
            font-size: 0.85rem;
        }
        
        /* Added styles for better card separation */
        .card-grid {
            grid-gap: 1.5rem; /* Increased gap between cards */
        }
        
        .card-divider {
            height: 1px;
            background-color: #f1f1f1;
            margin: 0.75rem 0;
        }
    </style>
</head>
  
<body class="bg-gray-100" style="font-family: 'Inter', sans-serif;">
    <div>
        <%- include('components/navigation') %>
        <div class="md:pl-48 flex flex-col flex-1">
            <main class="flex-1">
                <div class="py-6">
                    <div class="max-w-7xl pt-8 mx-auto px-4 sm:px-6 md:px-8">
                        <div class="lg:flex lg:items-center lg:justify-between mb-8">
                            <div class="min-w-0 flex-1">
                                <h2 class="text-2xl font-bold leading-7 text-gray-900 sm:truncate sm:text-3xl sm:tracking-tight">J4R (Join for Reward)</h2>
                                <p class="text-md font-medium text-gray-600">Take a look at the servers that have partnered with us, join to them and get rewards.</p>
                            </div>
                        </div>
                        
                        <% if (settings.api.client.j4r.enabled) { %>
                            <% if (settings.api.client.j4r.ads && settings.api.client.j4r.ads.length > 0) { %>
                                <!-- Changed to 4-column grid with responsive adjustments -->
                                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 card-grid">
                                    <% settings.api.client.j4r.ads.forEach(function(server) { %>
                                        <div class="server-card bg-white rounded-xl shadow-sm overflow-hidden border border-gray-100 flex flex-col">
                                            <div class="compact-card flex flex-col flex-grow">
                                                <div class="flex justify-between items-start mb-3">
                                                    <h3 class="compact-title font-bold text-gray-900"><%= server.name %></h3>
                                                    <span class="coin-badge">
                                                        <i class="fas fa-coins mr-1"></i>
                                                        <%= server.coins %>
                                                    </span>
                                                </div>
                                                
                                                <!-- Added a subtle divider -->
                                                <div class="card-divider"></div>
                                                
                                                <p class="compact-text text-gray-600 flex-grow">Join this server to earn rewards!</p>
                                                <div class="mt-auto pt-2 text-center">
                                                    <a href="<%= server.invite %>" target="_blank" class="transition inline-flex items-center rounded-md bg-black-600 px-3 py-2 text-sm font-semibold text-white hover:bg-gray-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-rose-600">
                                                        Join Server
                                                    </a>
                                                </div>
                                            </div>
                                        </div>
                                    <% }); %>
                                </div>
                            <% } else { %>
                                <div class="text-center py-12 bg-white rounded-lg shadow-md">
                                    <i class="fas fa-frown text-gray-400 text-5xl mx-auto mb-4"></i>
                                    <h3 class="text-xl font-medium text-gray-900 mb-2">No servers available</h3>
                                    <p class="text-gray-600">Check back later for new J4R server opportunities.</p>
                                </div>
                            <% } %>
                        <% } else { %>
                            <div class="text-center py-12 bg-white rounded-lg shadow-md">
                                <i class="fas fa-lock text-gray-400 text-5xl mx-auto mb-4"></i>
                                <h3 class="text-xl font-medium text-gray-900 mb-2">J4R is currently disabled</h3>
                                <p class="text-gray-600">This feature is not available at the moment.</p>
                            </div>
                        <% } %>
                    </div>
                </div>
            </main>
        </div>
    </div>
</body>
</html>